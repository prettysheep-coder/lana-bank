import { SetupAuthenticator } from "@/components/settings/authenticator/index"

export default {
  title: "components/Setting/SetupAuthenticatorDialog",
  component: SetupAuthenticator,
}

export const Default = () => {
  return (
    <>
      <SetupAuthenticator />
    </>
  )
}
