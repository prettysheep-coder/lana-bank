config {
	tags: ["current"],
	type: "table",
	description: "Each row represents a unique account.",
	assertions: {
		uniqueKey: ["account_id"],
		nonNull: ["account_id"],
	},
}

SELECT *

FROM ${ref("int_accounts")}
